package org.example.wallet.currencies;

import org.example.wallet.currencies.CurrencyTemplate;

public class EUR extends CurrencyTemplate {
    public EUR(long amount) {
        super("EUR", amount);
    }
}
